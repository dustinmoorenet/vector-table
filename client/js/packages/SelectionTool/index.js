import Package from '../../libs/Package';

export default class SelectionTool extends Package {
    defaultRoute(event) {

    }

    applyHandles(items) {
        // take the items and build a box around them
        // also make a red outline of each shape
    }
}
